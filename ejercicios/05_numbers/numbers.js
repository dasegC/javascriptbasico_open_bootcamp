let altura_cm = 170;
let altura_m = 1.70;
let peso_kg = 54.5;

let altura_redondeado = Math.ceil(altura_m);
//console.log(altura_redondeado)
let peso_redondeado = Math.floor(peso_kg);
//console.log(peso_redondeado);

let iguales = Number.MAX_VALUE + 1 === Number.MAX_VALUE;
//console.log(iguales);